# ── LOCAL (SQLite) ─────────────────────────────────
OPENAI_API_KEY=sk-votre-cle-api
JWT_SECRET=votre-secret-super-long
DATABASE_URL=sqlite:///./app.db

# ── PRODUCTION (GCP Cloud SQL) ─────────────────────
# Laissez DATABASE_URL vide en production. Le driver
# se connecte automatiquement via le socket Unix.
# CLOUD_SQL_CONNECTION_NAME=projet:europe-west1:ma-db
# DB_USER=armaelect
# DB_PASSWORD=ChangerMoi123!
# DB_NAME=armaelect
